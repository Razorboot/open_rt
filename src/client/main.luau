--# Services
local ReplicatedStorage = game:GetService("ReplicatedStorage")

--# Include
local OpenRT = ReplicatedStorage:WaitForChild("OpenRT")
local MaterialsContext = require(OpenRT:WaitForChild("MaterialsContext"))

--# Init OpenRT
